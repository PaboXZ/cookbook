{% extends '_layout_straight.html.twig' %}
{% block content %}
{% for recipe in recipes %}
<a href="{{ path('app_recipe_show', {id: recipe.id }) }}">{{ recipe.name }}</a>
<br>
{% endfor %}
{% if not recipes %}
Nie znaleziono przepisów dla kategorii {{ category }}
{% endif %}
{% endblock %}